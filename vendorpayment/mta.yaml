_schema-version: "3.2"
ID: vendorpayment
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: vendorpayment-destination-content
  type: com.sap.application.content
  requires:
  - name: vendorpayment-destination-service
    parameters:
      content-target: true
  - name: vendorpayment-repo-host
    parameters:
      service-key:
        name: vendorpayment-repo-host-key
  - name: vendorpayment-uaa
    parameters:
      service-key:
        name: vendorpayment-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: vendorpayment_html_repo_host
          ServiceInstanceName: vendorpayment-html5-service
          ServiceKeyName: vendorpayment-repo-host-key
          sap.cloud.service: vendorpayment
        - Authentication: OAuth2UserTokenExchange
          Name: vendorpayment_uaa
          ServiceInstanceName: vendorpayment-xsuaa-service
          ServiceKeyName: vendorpayment-uaa-key
          sap.cloud.service: vendorpayment
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: vendorpayment-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: vendorpayment-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - vendorpayment.zip
      name: vendorpayment
      target-path: resources/
- name: vendorpayment
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: vendorpayment-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: vendorpayment-destination-service
    service-plan: lite
- name: vendorpayment-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: vendorpayment-xsuaa-service
    service-plan: application
- name: vendorpayment-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: vendorpayment-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true

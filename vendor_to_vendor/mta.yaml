_schema-version: "3.2"
ID: vendortovendor
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: vendortovendor-destination-content
  type: com.sap.application.content
  requires:
  - name: vendortovendor-destination-service
    parameters:
      content-target: true
  - name: vendortovendor-repo-host
    parameters:
      service-key:
        name: vendortovendor-repo-host-key
  - name: vendortovendor-uaa
    parameters:
      service-key:
        name: vendortovendor-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: vendortovendor_html_repo_host
          ServiceInstanceName: vendortovendor-html5-service
          ServiceKeyName: vendortovendor-repo-host-key
          sap.cloud.service: vendortovendor
        - Authentication: OAuth2UserTokenExchange
          Name: vendortovendor_uaa
          ServiceInstanceName: vendortovendor-xsuaa-service
          ServiceKeyName: vendortovendor-uaa-key
          sap.cloud.service: vendortovendor
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: vendortovendor-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: vendortovendor-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - vendortovendor.zip
      name: vendortovendor
      target-path: resources/
- name: vendortovendor
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: vendortovendor-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: vendortovendor-destination-service
    service-plan: lite
- name: vendortovendor-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: vendortovendor-xsuaa-service
    service-plan: application
- name: vendortovendor-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: vendortovendor-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true

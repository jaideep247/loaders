_schema-version: "3.2"
ID: purchaseorder
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: purchaseorder-destination-content
  type: com.sap.application.content
  requires:
  - name: purchaseorder-destination-service
    parameters:
      content-target: true
  - name: purchaseorder-repo-host
    parameters:
      service-key:
        name: purchaseorder-repo-host-key
  - name: purchaseorder-uaa
    parameters:
      service-key:
        name: purchaseorder-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: purchaseorder_html_repo_host
          ServiceInstanceName: purchaseorder-html5-service
          ServiceKeyName: purchaseorder-repo-host-key
          sap.cloud.service: purchaseorder
        - Authentication: OAuth2UserTokenExchange
          Name: purchaseorder_uaa
          ServiceInstanceName: purchaseorder-xsuaa-service
          ServiceKeyName: purchaseorder-uaa-key
          sap.cloud.service: purchaseorder
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: purchaseorder-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: purchaseorder-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - purchaseorder.zip
      name: purchaseorder
      target-path: resources/
- name: purchaseorder
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: purchaseorder-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: purchaseorder-destination-service
    service-plan: lite
- name: purchaseorder-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: purchaseorder-xsuaa-service
    service-plan: application
- name: purchaseorder-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: purchaseorder-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
